# Rust Actix Web Benchmark Server (Template)

* **Language**: Rust
* **Framework**: Actix Web (`sqlx` / `tokio-postgres` / `mysql_async`)
* **Default Port**: `8006`

Template for adding new compiled language frameworks to the benchmark suite.
