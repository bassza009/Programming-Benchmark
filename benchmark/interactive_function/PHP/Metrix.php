<?php

$n = 1000;

// 2. เริ่มจับเวลา (ตรงตามข้อ 3.5 ในไฟล์วิจัย)


for ($i = 0; $i < $n; $i++) {
    for ($j = 0; $j < $n; $j++) {
        $sum = 0.0;
        for ($k = 0; $k < $n; $k++) {
            $sum += ($i * $n + $k) * ($k * $n + $j);
        }
        $result = $sum;
    }
}
